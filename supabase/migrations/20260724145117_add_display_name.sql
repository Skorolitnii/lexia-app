-- Имя пользователя: вводится на экране входа, показывается в Настройках →
-- Аккаунт. Часть доменной схемы (в settings, а не в auth.user_metadata),
-- чтобы редактироваться позже и синкаться вместе с остальными настройками.
-- Nullable: старым строкам имени неоткуда взяться, а RLS уже покрывает settings.
alter table settings
  add column display_name text;
